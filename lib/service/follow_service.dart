import 'package:flutter/material.dart';

class FollowService extends StatelessWidget {
  const FollowService({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}